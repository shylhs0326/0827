# superSCM 프로젝트 비교 및 작업 제안

## 1. 비교 대상

- 현재 프로젝트: `C:\Users\fujifilm\Downloads\superSCM-main (1)\superSCM-main`
- 비교 프로젝트: `C:\Users\fujifilm\Downloads\superSCM-main (2)\superSCM-main`
- 비교일: 2026-09-10

빌드 산출물인 `.next`, 설치 의존성인 `node_modules`, Git 내부 파일과 `outputs` 산출물은 기능 비교에서 제외했다.

정규화된 소스 기준으로 현재 프로젝트는 140개, 비교 프로젝트는 153개 파일이며, 동일 파일 25개, 내용이 다른 공통 파일 73개, 현재에만 있는 파일 42개, 비교 프로젝트에만 있는 파일 55개다.

## 2. 결론

비교 프로젝트를 현재 프로젝트에 통째로 덮어쓰면 안 된다. 비교 프로젝트에는 실데이터 분석 화면, 역할별 메뉴, Agent 진행 스트리밍 등 가져올 가치가 있는 기능이 있지만 다음 한계도 있다.

- 현재 프로젝트보다 단순한 Import 구현이 있어 그대로 교체하면 기존 검증·롤백 기능이 후퇴한다.
- Agent 대화 저장 구조가 기존 요구사항인 질문·답변 원자적 저장과 맞지 않는다.
- 엄격한 Agent JSON 파서를 느슨하게 만드는 코드가 포함되어 있다.
- 코드가 참조하는 일부 실데이터 View 생성 SQL이 저장소에 없다.
- Forecast 관련 관리자 화면 대부분은 실제 기능이 아닌 준비 중 안내 화면이다.

따라서 현재 프로젝트를 기준으로 유지하고, 비교 프로젝트의 필요한 기능만 순서대로 선별 병합한다.

## 3. 최우선 수정 사항

### 3.1 Agent 역할 타입 통일

현재 `lib/agent/tools.ts`는 Tool 역할을 `analyst | planner`로 정의한다. 반면 로그인 계층과 `app/(user)/agent/actions.ts`는 `ADMIN | USER`를 전달한다. 이 상태에서는 실제 화면에서 허용 Tool 목록이 비어 Agent가 Tool을 사용하지 못할 수 있다.

작업:

- [x] `lib/agent/tools.ts`의 별도 `AgentRole`을 제거한다.
- [x] `lib/menu.ts` 또는 공통 인증 모듈의 `AppRole = 'ADMIN' | 'USER'`를 사용한다.
- [x] 네 Tool의 `roles`를 실제 앱 역할로 변경한다.
- [x] `lib/agent/orchestrator.ts`의 실행 직전 2차 역할 검사도 같은 타입을 사용한다.
- [x] 실제 `USER`, `ADMIN` 각각 네 Tool이 노출·실행되는 테스트를 추가한다.
- [x] 알 수 없는 역할에서는 Tool이 하나도 노출되지 않는 테스트를 유지한다.

관련 파일:

- `lib/agent/tools.ts`
- `lib/agent/tools.test.ts`
- `lib/agent/orchestrator.ts`
- `lib/agent/orchestrator.test.ts`
- `app/(user)/agent/actions.ts`
- `lib/menu.ts`

### 3.2 실데이터 분석 화면 추가

비교 프로젝트에는 현재 프로젝트에 없는 수요 패턴과 OL 정확도 화면이 있다.

추가할 화면:

- [x] `app/(user)/analysis/demand-profile/page.tsx`
- [x] `components/analysis/demand-profile-table.tsx`
- [x] `app/(user)/analysis/model-comparison/page.tsx`
- [x] `components/analysis/ol-accuracy-table.tsx`

조회 계층:

- [x] `getItemDemandProfiles()`를 화면용 전체 목록 조회 함수로 추가한다.
- [x] `getItemDemandKpi()`를 추가한다.
- [x] `getOlAccuracyFy()`를 추가한다.
- [x] 화면용 `getOlAccuracy()` 전체 조회와 Agent용 기종·회계연도 필터 조회를 충돌 없이 분리한다.
- [x] 기존 Agent용 `getShipmentTrend(itemCode)`, `getDemandProfile(itemCode)`, `getOlAccuracy(modelBase, fy)`, `getBomRequirement(modelBase)` 계약은 유지한다.
- [x] 화면에서 평균·합계·판정 값을 새로 계산하지 않고 SQL View 결과를 표시한다.
- [x] 조회 오류와 정상적인 빈 결과를 별도로 표시한다.
- [x] `null`은 0으로 바꾸지 않고 사유 코드와 함께 표시한다.

필요한 View:

- `analytics.v_item_demand_profile`
- `analytics.v_item_demand_kpi`
- `analytics.v_shipment_trend`
- `analytics.v_ol_accuracy`
- `analytics.v_ol_accuracy_fy`
- `analytics.v_bom_requirement_x`

주의: 비교 프로젝트는 위 View를 사용하지만 이를 생성한다고 주석에 적은 `07-deprecate-and-agent.sql` 파일이 없다. 실제 Supabase에 View가 존재하는지 먼저 확인하고, 없다면 별도의 전진 마이그레이션으로 생성해야 한다.

## 4. 화면 구조와 메뉴 정비

### 4.1 기본 진입 경로

비교 프로젝트는 `/`에서 `/dashboard`로 이동한다.

- [x] `app/page.tsx`에서 `/dashboard`로 리다이렉트한다.
- [x] 현재 `app/(user)/page.tsx`의 역할을 `/dashboard`로 이전한다.
- [x] 기존 `/workflow` 링크는 삭제하지 말고 호환 경로로 유지한다.

### 4.2 역할별 메뉴

- [x] 메뉴 상수를 `USER_MENU`, `ADMIN_MENU`로 정리한다.
- [x] `menuForRole(role)`로 USER와 ADMIN 메뉴를 조합한다.
- [x] 메뉴 아이콘을 Sidebar의 별도 경로 맵이 아니라 각 `MenuItem`에 둔다.
- [x] USER 메뉴에 `/dashboard`, 수요 패턴, OL 정확도, 리드타임, 재고 소진, Agent를 포함한다.
- [x] ADMIN에게 USER 메뉴와 ADMIN 메뉴를 함께 표시한다.
- [ ] 기존 경로를 사용하는 북마크가 깨지지 않도록 필요한 리다이렉트를 둔다.

관련 파일:

- `lib/menu.ts`
- `components/shell/sidebar.tsx`
- `components/shell/topbar.tsx`
- `app/(user)/layout.tsx`
- `app/(admin)/layout.tsx`
- `app/(user)/dashboard/page.tsx`
- `app/page.tsx`

## 5. Agent 화면 개선

비교 프로젝트의 Agent 화면은 서버가 최종 답변을 반환할 때까지 기다리는 방식에서 SSE 기반 진행 표시 방식으로 확장되어 있다.

선별 적용할 기능:

- [x] `app/api/agent/stream/route.ts`를 추가한다.
- [x] 인증 후 질문 길이와 빈 질문을 서버에서 다시 검사한다.
- [x] 계획, Tool 시작, Tool 종료, 답변 정리, 숫자 검증, 재생성 단계를 진행 이벤트로 보낸다.
- [x] Guardrail을 통과하기 전의 답변 본문은 클라이언트에 보내지 않는다.
- [ ] 사용자가 실행을 중지할 수 있도록 `AbortController`를 연결한다.
- [ ] 한글 입력 조합 중 Enter가 질문 전송으로 처리되지 않도록 한다.
- [ ] `answer-card.tsx`로 Structured Answer 렌더링을 분리한다.
- [x] Tool Trace는 접을 수 있게 유지한다.
- [ ] 기존 요구사항인 예시 질문 4개는 비교 프로젝트에 없으므로 현재 기능을 유지한다.
- [x] API key와 Tool 원본 데이터 전체를 클라이언트로 보내지 않는다.

관련 파일:

- `app/api/agent/stream/route.ts`
- `app/(user)/agent/chat-form.tsx`
- `app/(user)/agent/answer-card.tsx`
- `app/(user)/agent/actions.ts`
- `lib/agent/orchestrator.ts`

### 5.1 Agent 실행 결과 보강

현재 프로젝트의 `runAgent()` 결과에는 답변과 Trace만 포함된다. 비교 프로젝트처럼 다음 정보를 반환하고 저장 계층까지 전달할 필요가 있다.

- [x] LLM token usage
- [x] Guardrail 검사 수치 개수
- [ ] 불일치 숫자 목록
- [ ] 재생성 수행 여부
- [x] 설정 여부와 처리 오류
- [x] Tool 단계 진행 이벤트

현재의 엄격한 숫자 검증 원칙은 유지한다. 질문에 들어 있던 숫자를 자동 허용하는 비교 프로젝트 방식은 숫자 검증 우회 범위를 넓힐 수 있으므로 그대로 복사하지 않는다.

## 6. 인증과 접근 제어 보강

비교 프로젝트에서 선별 적용할 인증 기능:

- [x] `safeNextPath()`로 외부 URL 리다이렉트를 차단한다.
- [x] `/auth/callback`에서 Supabase 인증 코드를 세션으로 교환한다.
- [x] API Route용 `requireSignedIn()`을 추가해 redirect 대신 401/403을 반환할 수 있게 한다.
- [x] middleware에서 로그인 여부뿐 아니라 `core.app_user.active`와 ADMIN 경로 권한을 검사한다.
- [x] User Layout과 Admin Layout에 검증된 profile을 전달한다.
- [x] 상단 바에 사용자명과 역할을 표시한다.
- [x] 현재 구현의 프로필 조회 장애와 비활성 사용자를 구분하는 오류 처리는 유지한다.

관련 파일:

- `middleware.ts`
- `lib/auth.ts`
- `lib/auth-policy.ts`
- `app/auth/callback/route.ts`
- `app/(auth)/login/actions.ts`
- `lib/auth-actions.ts`

## 7. 데이터베이스 마이그레이션 전략

비교 프로젝트에는 다음 마이그레이션이 추가되어 있다.

- STEP 5: SKU 수요 프로파일
- STEP 6: Baseline Forecast
- STEP 7: Backtest 및 Champion 모델
- STEP 16: Agent 대화 저장

### 7.1 STEP 5~7

- [ ] 실제 운영 DB의 현재 migration history와 객체 존재 여부를 먼저 확인한다.
- [ ] 비교 프로젝트의 과거 타임스탬프 파일을 그대로 복사하지 않는다.
- [ ] 현재 프로젝트보다 뒤의 새 타임스탬프로 전진 마이그레이션을 작성한다.
- [ ] STEP 5의 입력이 실제 `fact_shipment`/실데이터 View인지, 과거 `core.v_train_demand`인지 결정한다.
- [ ] STEP 6·7이 현재 실데이터 구조에서 실행 가능한지 검증한다.
- [ ] Forecast 관리자 화면은 실제 함수와 데이터가 연결되기 전까지 준비 중 화면임을 명시한다.
- [ ] 모든 core 테이블에 fail-closed RLS와 ADMIN mutation 정책을 적용한다.

### 7.2 Agent 대화 저장

비교 프로젝트의 Agent migration으로 현재 migration을 교체하지 않는다.

현재 프로젝트가 더 잘 지키는 사항:

- 질문과 답변을 별도 `role`, `content` 메시지로 저장한다.
- `save_agent_turn` RPC 안에서 질문과 답변을 한 transaction으로 저장한다.
- `answer`, `tool_trace`, `usage`, `guardrail`을 별도로 보존한다.
- 본인 대화만 읽고 쓰며 ADMIN은 전체 조회할 수 있다.

추가 개선:

- [x] 현재 `runAgent()`의 usage와 guardrail 결과를 `saveTurn()`에 실제로 전달한다.
- [ ] 대화 이어쓰기가 필요하면 기존 RPC에 nullable conversation ID를 추가한다.
- [ ] 기존 대화를 이어쓸 때도 소유자 검사를 RPC 내부에서 수행한다.
- [x] 저장 실패가 Agent 답변 자체를 없애지 않는 현재 동작을 유지한다.

## 8. 현재 프로젝트에서 유지해야 할 구현

다음은 비교 프로젝트 코드로 교체하지 않는다.

- [ ] 강화된 Import parsing, validation, staging, approval, rollback 구현
- [ ] Import의 natural key와 source record ID 처리
- [ ] Excel 수식 주입 방어가 포함된 오류 CSV 생성
- [ ] 프로필 조회 인프라 오류와 비활성 사용자를 구분하는 인증 오류 처리
- [ ] strict Agent JSON Schema와 필드 누락 거부 동작
- [ ] 질문·답변을 한 transaction으로 저장하는 `save_agent_turn` RPC
- [ ] Tool 결과에 없는 숫자를 허용하지 않는 현재 Guardrail 정책
- [ ] 기존 Agent 예시 질문 4개

## 9. 낮은 우선순위 또는 선택 작업

비교 프로젝트에만 있지만 현재는 실제 기능이 거의 없는 화면:

- `/admin/demand`
- `/admin/forecast-models`
- `/admin/forecast-runs`
- `/admin/backtest-runs`
- `/admin/champion-models`
- `/admin/settings`

이 화면들은 메뉴 완성도를 위해 추가할 수 있지만, 대부분 `준비 중` 또는 `NoRealDataNotice`를 표시한다. 실제 DB 기능보다 먼저 추가할 필요는 없다.

`components/chart/forecast-overlay-chart.tsx` 역시 현재는 자식 요소를 감싸는 껍데기뿐이므로 실제 차트 요구가 생길 때 구현한다.

## 10. 권장 작업 순서

### 1차 — 즉시 오류 방지

1. Agent 역할을 `ADMIN | USER`로 통일한다.
2. 실제 역할 기준 Tool 노출·실행 테스트를 추가한다.
3. Agent usage와 guardrail 전달 누락을 보완한다.

### 2차 — 사용자 분석 기능

1. 실데이터 View 존재 여부를 확인한다.
2. 수요 패턴 조회 함수와 화면을 추가한다.
3. OL 정확도 조회 함수와 화면을 추가한다.
4. 리드타임·재고 데이터가 없으면 폐기된 더미 View 대신 명시적인 안내를 표시한다.

### 3차 — 화면 구조와 인증

1. `/dashboard`와 루트 리다이렉트를 추가한다.
2. 역할 기반 통합 메뉴와 Sidebar를 적용한다.
3. middleware의 active/role 검사를 강화한다.
4. 로그인 callback과 API용 인증 함수를 추가한다.

### 4차 — Agent 사용성

1. 진행 이벤트를 orchestrator에 추가한다.
2. SSE Route를 추가한다.
3. 중지, 진행 단계, 다중 Turn 표시와 AnswerCard를 적용한다.
4. 현재 예시 질문과 데이터 비노출 원칙을 유지한다.

### 5차 — Forecast/Backtest

1. STEP 5~7 SQL을 현재 실데이터 계약에 맞게 다시 검토한다.
2. 새 전진 마이그레이션으로 적용한다.
3. 실제 데이터 연결 후 관리자 화면을 활성화한다.

## 11. 단계별 검증 기준

각 단계가 끝날 때 다음 명령을 실행한다.

```powershell
npm.cmd test
npx.cmd tsc --noEmit
npm.cmd run build
```

추가 확인:

- USER와 ADMIN이 허용된 네 Agent Tool을 실제로 사용할 수 있는가?
- 미허용 Tool을 이름으로 직접 요청해도 실행되지 않는가?
- 수요 패턴과 OL 정확도 화면의 행 수가 DB View와 같은가?
- 계산 불가 값이 0이 아니라 `null + reason`으로 표시되는가?
- Agent 답변의 모든 숫자가 ToolResult.numbers로 검증되는가?
- Agent 대화 저장 실패 시에도 이미 생성된 답변이 표시되는가?
- 일반 사용자가 다른 사용자의 대화를 조회할 수 없는가?
- 환경변수가 없을 때 `/agent`만 비활성화되고 다른 화면은 정상 동작하는가?
- 클라이언트 번들이나 응답에 `OPENAI_API_KEY`가 포함되지 않는가?

## 12. 비교 당시 검증 결과

현재 프로젝트:

- `npm test`: 120건 통과, 실패 0건
- `npx tsc --noEmit --incremental false`: 통과
- Agent와 관련된 미커밋 변경이 남아 있음

비교 프로젝트:

- Git 저장소가 아닌 파일 스냅샷
- 로컬 `node_modules` 없음
- `npm test`: 72건 중 71건 통과, 1건 실패
- 실패 원인: 설치된 `lucide-react`를 찾지 못한 환경 문제
- TypeScript 검사는 의존성이 설치되지 않아 완료하지 못함

## 13. 최종 권고

현재 프로젝트를 기준 저장소로 유지한다. 비교 프로젝트에서 가장 먼저 가져올 것은 실데이터 분석 화면 자체가 아니라 Agent 역할 통일과 조회 계약 정리다. 그다음 수요 패턴·OL 정확도 화면, 역할 기반 메뉴·인증, Agent 스트리밍 순으로 적용한다. DB 마이그레이션과 현재 Agent 저장 구조는 덮어쓰지 말고 새 전진 마이그레이션과 호환 확장으로 처리한다.
