# 월간 발주계획 시스템 디자인 시스템

> 버전: 1.0  
> 기준일: 2026-08-28  
> 적용 대상: 월간 발주계획, 수요·재고·마스터 분석, 보고자료 화면 전체

## 1. 디자인 방향

이 시스템은 해외 생산법인의 월간 발주계획을 빠르고 정확하게 검토하는 운영 도구다. 디자인은 다음 세 가지 인상을 동시에 전달해야 한다.

1. **신뢰감** — 발주량·재고·금액처럼 실수가 비용으로 이어지는 정보를 안정적으로 보여준다.
2. **정밀함** — 데이터의 기준월, 상태, 예외, 계산 결과를 한눈에 구분한다.
3. **집중력** — 장식보다 업무 흐름과 다음 행동을 우선한다.

참고한 Equitas 스타일의 미니멀한 엔터프라이즈 감성을 현재 제품의 네이비 운영 콘솔에 결합한다. 결과물은 화려한 마케팅 페이지가 아니라, **차분한 전략·운영 시스템**이어야 한다.

### 핵심 원칙

- 화면마다 새로운 색과 컴포넌트를 만들지 않는다. 아래 토큰과 기존 공통 클래스를 우선 사용한다.
- 정보 위계는 `페이지 제목 → 현재 상태 → 핵심 수치 → 상세 데이터 → 다음 행동` 순서를 따른다.
- 색상은 장식이 아니라 상태와 의미를 표현할 때 사용한다.
- 카드와 표를 과도하게 겹치지 않는다. 하나의 영역은 하나의 질문에 답해야 한다.
- 계산 결과와 원천 입력을 시각적으로 구분한다.
- 한국어 문장은 짧고 명확하게 쓴다. 영문은 단계명·제품 용어·보조 라벨에 제한적으로 사용한다.

## 2. 브랜드 보이스

### 문장 톤

- 전문적이지만 딱딱하지 않게 쓴다.
- 사용자가 무엇을 해야 하는지 문장 안에서 드러낸다.
- 오류는 사용자를 탓하지 않고, 문제와 해결 방법을 함께 말한다.

좋은 예:

- `수요 확정이 완료되었습니다.`
- `3개 품목의 리드타임이 없어 발주량을 계산할 수 없습니다.`
- `마스터를 먼저 검증한 뒤 발주량을 계산하세요.`

피할 표현:

- `처리 실패`
- `데이터 오류`
- `확인 필요`처럼 원인이나 행동이 없는 문구

### 상태 문구

상태 배지는 한 단어 또는 짧은 구로 쓴다.

`진행 전` · `진행 중` · `완료` · `조건부` · `검토 필요` · `오류` · `참고`

## 3. 디자인 토큰

현재 `app/globals.css`의 CSS 변수를 기준 토큰으로 사용한다. 새 화면에서 색상 값을 직접 작성하지 말고 의미가 있는 변수를 사용한다.

### 색상

| 역할 | 토큰 | 값 | 사용처 |
|---|---|---:|---|
| 앱 배경 | `--canvas` | `#f5f7fb` | 전체 페이지 배경 |
| 기본 표면 | `--surface` | `#ffffff` | 카드, 표, 입력 영역 |
| 주요 텍스트 | `--ink` | `#182230` | 제목, 본문, 숫자 |
| 보조 텍스트 | `--muted` | `#718096` | 설명, 메타데이터, 비활성 상태 |
| 브랜드 네이비 | `--navy` | `#17365d` | 사이드바, 중요 제목, 강조 텍스트 |
| 주요 액션 | `--blue` | `#2f6fed` | 버튼, 활성 탭, 진행 중 상태 |
| 연한 주요색 | `--pale-blue` | `#edf4ff` | 선택 배경, 안내, 정보 강조 |
| 성공 | `--green` | `#1f9d68` | 완료, 정상, 개선 |
| 연한 성공색 | `--pale-green` | `#eaf8f1` | 성공 배지·콜아웃 배경 |
| 주의 | `--amber` | `#bd7211` | 조건부, 검토 필요, 사용자 확인 |
| 연한 주의색 | `--pale-amber` | `#fff6e4` | 주의 배지·콜아웃 배경 |
| 오류·위험 | `--red` | `#c2413d` | 오류, 부족, 위험 |
| 연한 오류색 | `--pale-red` | `#fff0ef` | 오류 배지·콜아웃 배경 |
| 경계선 | `--line` | `#e3e8ef` | 카드, 표, 구분선 |

### 색상 사용 규칙

- 파란색은 `현재 위치`와 `사용자가 실행할 수 있는 행동`에 사용한다.
- 초록색은 이미 끝났거나 정상인 상태에만 사용한다.
- 앰버는 사용자의 판단이 필요한 상태에 사용한다. 단순 장식으로 쓰지 않는다.
- 빨간색은 실제 위험 또는 오류에만 사용한다. 일반적인 숫자 강조에는 쓰지 않는다.
- 본문 텍스트와 배경의 대비는 WCAG AA 수준을 목표로 한다.

### 타이포그래피

기본 글꼴은 `Noto Sans KR`이다. 숫자와 영문도 같은 폰트를 사용해 한 화면의 리듬을 유지한다.

| 단계 | 크기 | 굵기 | 용도 |
|---|---:|---:|---|
| Display | 32px | 800 | 대시보드의 대표 메시지, 한 화면에 최대 1개 |
| Page title | 25–26px | 800 | 페이지 제목 |
| Section title | 15–18px | 700–800 | 카드·섹션 제목 |
| Body | 13–14px | 400–500 | 설명, 일반 정보 |
| Data | 12–14px | 500–700 | 표와 입력 데이터 |
| Label | 10–12px | 700–800 | 필드 라벨, eyebrow, 상태 |
| Caption | 10–11px | 400–600 | 기준일, 출처, 보조 설명 |

규칙:

- 제목은 `letter-spacing: -0.5px`에서 `-1px` 범위로 조정한다.
- 영문 eyebrow는 대문자, `letter-spacing: 1.2px`, 파란색을 사용한다.
- 숫자 지표는 굵게 표시하되, 숫자보다 단위를 작게 보여준다.
- 표 안의 숫자는 오른쪽 정렬하고, 수량·금액·일수의 단위를 열 제목 또는 값 뒤에 명시한다.

### 간격

8px 단위를 기본으로 한다.

| 토큰 | 값 | 사용 예 |
|---|---:|---|
| `xs` | 4px | 아이콘과 짧은 텍스트 |
| `sm` | 12px | 라벨과 입력, 작은 그룹 |
| `base` | 8px | 기본 내부 간격 |
| `md` | 24px | 카드 내부, 주요 그룹 |
| `lg` | 48px | 섹션 간 여백 |
| `xl` | 80px | 큰 페이지 구간 |

추가 기준:

- 카드 내부 기본 패딩: 18–24px
- 카드와 카드 사이: 15–18px
- 주요 섹션 사이: 24–32px
- 콘텐츠 좌우 여백: 데스크톱 42–64px, 모바일 18px
- 클릭 가능한 요소의 최소 높이: 34px, 주요 버튼은 40px 이상

### 모서리와 그림자

- 일반 카드: `border-radius: 14px`
- 입력·작은 버튼: `7–9px`
- 상태 배지·단계 표시: `999px`
- 기본은 1px 경계선이다. 그림자는 선택된 카드나 떠 있는 요소에만 사용한다.
- 그림자는 낮고 넓게 사용한다. 강한 검정 그림자, 과도한 글로우, 유리 질감은 사용하지 않는다.

## 4. 레이아웃 구조

### 데스크톱

```text
┌──────────────┬──────────────────────────────────────────┐
│ 고정 사이드바 │ 상단 바: 현재 단계 / 기준월 / 연결 상태     │
│ 250px         ├──────────────────────────────────────────┤
│ 브랜드        │ 콘텐츠: 진행 흐름 → 페이지 헤더 → 본문     │
│ 업무 단계     │                                          │
│ 분석 링크     │                                          │
│ 하단 상태     │                                          │
└──────────────┴──────────────────────────────────────────┘
```

- 사이드바는 제품의 기준점이므로 모든 업무 화면에서 동일한 위치와 폭을 유지한다.
- 상단 바는 현재 단계와 기준월을 항상 보여준다.
- 콘텐츠는 `max-width: 1440px` 안에서 가운데 정렬한다.
- 분석 화면은 공통 프레임을 사용하되, 분석 전용 탭과 표 밀도를 허용한다.

### 모바일

- 사이드바는 상단 영역으로 전환한다.
- 업무 단계는 가로 스크롤 가능한 단계 목록으로 유지한다.
- 2열 이상 그리드는 1열로 전환한다.
- 표는 카드로 억지 변환하지 말고 가로 스크롤을 허용한다.
- 제목과 주요 행동은 세로로 쌓되, 행동 버튼은 화면 폭에 맞게 확장할 수 있다.

## 5. 공통 컴포넌트 규칙

### 사이드바와 브랜드

- 배경은 `#102b4b` 계열의 짙은 네이비로 유지한다.
- 브랜드 마크는 블루 정사각형 안에 `OP`를 표시한다. 별도 로고가 생기면 같은 38px 영역에 맞춘다.
- 내비게이션은 아이콘 + 한국어 라벨 + 선택 상태로 구성한다.
- 현재 단계는 블루 배경, 완료 단계는 초록 텍스트와 체크 아이콘으로 표현한다.
- 분석 링크는 업무 플로우와 시각적으로 구분하되, 동일한 내비게이션 패턴을 사용한다.

### 상단 바

- 왼쪽: 영문 eyebrow와 현재 페이지 제목
- 오른쪽: 환경·연결 상태 배지와 기준월
- `LOCAL PROTOTYPE`, `SUPABASE LIVE`처럼 시스템 상태를 숨기지 않는다.
- 상태 배지는 알림처럼 크게 만들지 않고 작은 pill 형태로 유지한다.

### 진행 단계

- 전체 업무 흐름은 `progress-wrap` 안에 둔다.
- 완료는 초록색 채움 원 + 체크, 현재는 블루 외곽선 + 약한 링, 예정은 회색 외곽선으로 표현한다.
- 단계명은 짧게 유지한다. 상세 설명은 페이지 본문에서 한다.
- 클릭 가능한 단계는 hover/focus 상태를 제공한다.
- 단계 이동으로 데이터가 사라질 수 있으면 이동 전에 확인을 요청한다.

### 페이지 헤더

페이지 헤더는 다음 구조를 따른다.

```text
eyebrow
제목
한 문장 설명
                                  주요 행동 버튼
```

- 제목은 무엇을 관리·검토하는지 바로 말한다.
- 설명은 목적 또는 현재 조건을 한 문장으로 설명한다.
- 주요 버튼은 한 개를 원칙으로 하며, 보조 버튼은 outline 또는 ghost로 낮춘다.

### 카드

- 카드는 독립된 질문이나 작업 단위를 담을 때만 사용한다.
- 카드 제목 오른쪽에는 기준월, 행 수, 출처처럼 보조 메타데이터를 둔다.
- 카드 안에 카드가 반복해서 들어가는 구조는 피한다.
- KPI 카드는 값, 라벨, 보조 설명의 3단 구조를 유지한다.
- 인터랙티브 KPI는 hover/focus 시 약간 떠오르며, 어디로 이동하는지 힌트를 제공한다.

### 버튼

| 유형 | 클래스 | 사용처 |
|---|---|---|
| Primary | `.button.primary` | 저장, 확정, 계산 실행, 다음 단계 |
| Secondary | `.button` | 취소, 이전, 보조 이동 |
| Ghost | `.button.ghost` | 필터, 상세 보기, 가벼운 보조 행동 |

- 버튼 라벨은 동사로 시작한다: `수요 확정`, `발주량 계산`, `보고서 다운로드`.
- destructive 행동은 빨간색 버튼으로 남용하지 말고, 확인 대화상자에서 위험을 설명한다.
- 비활성 버튼은 이유를 tooltip에만 숨기지 말고 주변 설명이나 안내문에 표시한다.

### 배지와 콜아웃

- `.tag.blue`, `.tag.green`, `.tag.amber`, `.tag.red`, `.tag.gray`만 사용한다.
- 배지에는 상태명만 쓴다. 긴 설명은 옆의 텍스트로 분리한다.
- 콜아웃은 사용자가 조치를 취해야 할 때만 사용한다.
- 정보성 안내는 파란색, 판단 필요는 앰버, 실제 오류는 빨간색을 사용한다.

### 표

- 표는 입력·검증·비교처럼 행 단위 확인이 필요한 정보에 사용한다.
- 헤더는 연한 회색 배경과 짙은 회색 텍스트로 구분한다.
- 행 hover는 아주 옅은 회색으로만 표시한다.
- 숫자는 오른쪽, 상태·코드·이름은 왼쪽 정렬한다.
- 열 제목이 길어지면 줄바꿈보다 짧은 표시명과 보조 설명을 사용한다.
- 빈 결과와 조회 오류를 구분한다.
  - 오류: `조회에 실패했습니다: {error}`
  - 빈 결과: `표시할 데이터가 없습니다.`
- 계산 불가 값은 `— + reason_code` 형식으로 표시하고, 임의의 숫자로 대체하지 않는다.

### 입력과 업로드

- 입력 라벨은 항상 입력 위에 둔다.
- 필수 필드는 라벨 옆에 `필수`를 표시한다.
- 유효성 검사는 제출 후가 아니라 가능한 경우 필드 단위로 즉시 제공한다.
- 업로드는 `업로드 → 미리보기 → 검증 결과 → 반영`의 4단계로 보여준다.
- 오류 행 수, 경고 행 수, 반영 가능 행 수를 따로 표시한다.

## 6. 업무 흐름별 표현

### 전체 현황

첫 화면은 사용자가 현재 어디까지 완료했으며 무엇을 해야 하는지 답해야 한다.

- 상단: 계획명, 기준월, 전체 상태
- KPI: 수요 확정, 재고 입력, 마스터 검증, 예외 품목, 총 발주금액
- 중앙: 6단계 업무 플로우
- 하단: 최근 경고, 다음 행동, 데이터 기준일

### 수요·재고·마스터

- 원천 데이터 입력과 시스템 검증 결과를 같은 표 안에서 시각적으로 구분한다.
- 상태는 수치보다 먼저 보여주고, 수치는 오른쪽 정렬한다.
- 경고는 행 전체를 빨갛게 칠하지 말고 상태 배지와 짧은 사유로 표시한다.
- 사용자가 직접 수정한 값은 수정 가능 상태를 명확히 보여준다.

### 발주량 계산

- 계산 전: 입력 조건과 검증 상태를 먼저 보여준다.
- 계산 중: 실행 중 상태와 재실행 가능 여부를 알려준다.
- 계산 후: 총량·총액·예외 수를 상단에 보여주고, 상세 결과와 계산 사유를 아래에 둔다.
- 수동 조정값은 계산값과 다른 색 또는 `수동 조정` 배지로 구분한다.

### 보고자료

- 보고 화면은 운영 입력 화면보다 밀도를 낮춘다.
- 경영진이 먼저 보는 순서인 `총액 → 수량 → 주요 변동 → 예외 → 세부표`를 따른다.
- 다운로드 버튼은 파일 형식과 기준월을 함께 표시한다.

## 7. 인터랙션과 상태

### 상태 전이

모든 주요 작업은 다음 상태를 고려한다.

`기본 → hover → focus → 실행 중 → 성공 / 실패`

- 실행 중에는 버튼 라벨을 유지하면서 spinner 또는 `처리 중…`을 표시한다.
- 성공은 초록색 보조 메시지와 다음 가능한 행동을 함께 보여준다.
- 실패는 원인, 영향 범위, 재시도 또는 수정 방법을 함께 보여준다.
- 자동 저장이 없는 화면에서는 페이지를 떠날 때 미저장 변경을 경고한다.

### 애니메이션

- 전환 시간은 150–200ms로 짧게 유지한다.
- hover 시 카드는 최대 3px만 이동한다.
- 숫자가 튀거나 전체 레이아웃이 흔들리는 애니메이션은 사용하지 않는다.
- `prefers-reduced-motion: reduce` 환경에서는 변환과 전환을 줄인다.

## 8. 접근성 기준

- 모든 버튼과 링크는 키보드로 접근 가능해야 한다.
- focus 상태를 제거하지 않는다. 기본 focus 링이 배경에 묻히면 블루 외곽선과 box-shadow를 사용한다.
- 색상만으로 상태를 전달하지 않는다. 아이콘, 라벨, 사유를 함께 제공한다.
- 아이콘 버튼에는 `aria-label`을 제공한다.
- 표에는 의미 있는 `caption` 또는 주변 제목을 제공한다.
- 모바일 가로 스크롤 표는 스크롤 가능하다는 안내를 제공한다.
- 본문 텍스트는 12px 미만으로 내리지 않는다. eyebrow와 caption만 예외로 둔다.

## 9. 구현 규칙

- 스타일은 `app/globals.css`의 전역 클래스와 CSS 변수로 관리한다.
- 새 CSS 프레임워크, CSS Modules, styled-components를 추가하지 않는다.
- 새 컴포넌트는 기존 클래스(`app-shell`, `sidebar`, `topbar`, `content`, `card`, `grid`, `tag`, `button`, `analysis-*`)를 우선 재사용한다.
- 반복되는 시각 패턴은 새 클래스 하나로 추출하고, 페이지별 임의 스타일을 최소화한다.
- 계산식은 화면 컴포넌트가 아니라 모델·조회 계층에 둔다. 화면은 결과와 사유를 표현한다.
- 화면 문구와 주석은 한국어로 작성한다. 변수명과 타입명은 영어를 사용한다.

## 10. 적용 우선순위

전반적인 리디자인은 다음 순서로 적용한다.

1. 색상·글꼴·간격 토큰 정리
2. 사이드바·상단 바·콘텐츠 폭 통일
3. 페이지 헤더·카드·버튼·배지 통일
4. 진행 단계와 상태 표현 통일
5. 표·입력·업로드 상태 통일
6. 모바일 레이아웃과 키보드 접근성 보완
7. hover, focus, loading, error 상태 점검

각 단계가 끝날 때마다 데스크톱과 모바일에서 대표 화면 1개씩 확인한다. 한 번에 모든 화면을 다시 만들지 말고, `전체 현황 → 수요 확정 → 분석 화면` 순서로 기준 화면을 만든 뒤 나머지 화면에 확장한다.

## 11. 체크리스트

새 화면을 만들거나 수정할 때 다음을 확인한다.

- [ ] 현재 단계와 기준월이 상단에 보이는가?
- [ ] 제목·설명·주요 행동의 위계가 명확한가?
- [ ] 색상이 상태의 의미와 일치하는가?
- [ ] 숫자와 단위가 혼동되지 않는가?
- [ ] 빈 결과와 조회 오류를 구분하는가?
- [ ] 계산 불가 값을 임의의 숫자로 표시하지 않는가?
- [ ] 표와 입력이 모바일에서 사용할 수 있는가?
- [ ] 모든 인터랙티브 요소에 hover와 focus 상태가 있는가?
- [ ] 로딩·성공·실패 상태가 정의되어 있는가?
- [ ] 기존 공통 클래스와 토큰을 재사용했는가?
